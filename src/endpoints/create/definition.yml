create:
  handler: src/endpoints/create/post.handler
  package:
    include:
      - "src/endpoints/create/**"
  events:
    - http:
        path: "/create"
        method: post
  iamRoleStatements:
    - Effect: Allow
      Action:
        - dynamodb:PutItem
      # TODO: Verify permissions are working properly. This function SHOULD NOT be able to run.
      #          - dynamodb:GetItem
      Resource:
        - "Fn::GetAtt": [ authTable, Arn ]
